# ── API Configuration ──
VITE_API_BASE_URL=http://localhost:8000/api

# ── Tenant Token ──
# This token identifies which tenant (storefront) is making the request.
# Get it from your backend's tenants table → tenant_token column.
VITE_TENANT_TOKEN=

# ── Secure Storage Key ──
VITE_STORAGE_SECRET=
